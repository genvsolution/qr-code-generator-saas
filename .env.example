# .env.example
#
# This file contains example environment variables required for the QR Code Genius application.
# It should be copied to a file named '.env' and filled with actual values for local development.
# For production, these variables should be set directly in the environment or via a secure secrets management system.

# --- Application Configuration ---
# The name of the Flask application file.
FLASK_APP=app.py
# The Flask environment. Set to 'development' for local development, 'production' for deployment.
FLASK_ENV=development
# Set to 'True' for development mode (enables debug features, auto-reloading). Set to 'False' for production.
DEBUG=True
# A strong, random secret key used for session management, CSRF protection, and other security-related functions.
# GENERATE A NEW, LONG, AND RANDOM STRING FOR PRODUCTION!
SECRET_KEY="your_super_secret_and_long_random_key_here_replace_me_in_production_1234567890"

# --- Database Configuration (PostgreSQL) ---
# The connection string for your PostgreSQL database.
# Format: postgresql://user:password@host:port/database_name
# Example for local PostgreSQL: postgresql://qruser:qrpass@localhost:5432/qrgenius_db
DATABASE_URL="postgresql://qruser:qrpass@localhost:5432/qrgenius_db"

# --- Email Configuration (for password reset, user notifications) ---
# SMTP server hostname (e.g., smtp.gmail.com, smtp.mailtrap.io)
MAIL_SERVER=smtp.mailtrap.io
# SMTP server port (e.g., 587 for TLS, 465 for SSL)
MAIL_PORT=2525
# Enable or disable STARTTLS (True for most modern SMTP servers)
MAIL_USE_TLS=True
# Enable or disable SSL (usually one of MAIL_USE_TLS or MAIL_USE_SSL is True, not both)
MAIL_USE_SSL=False
# Username for the SMTP server (your email address)
MAIL_USERNAME=your_email@example.com
# Password for the SMTP server
MAIL_PASSWORD=your_email_password
# The default sender email address for system-generated emails
MAIL_DEFAULT_SENDER="QR Code Genius <noreply@qrgenius.com>"

# --- QR Code Storage Configuration ---
# Path on the local filesystem where generated QR code images will be stored.
# Ensure this directory exists and the application has write permissions.
QR_CODE_STORAGE_PATH=./qr_codes_storage

# --- Cloud Storage (AWS S3) - Uncomment and configure if using S3 instead of local filesystem ---
# AWS_ACCESS_KEY_ID=your_aws_access_key_id
# AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET_NAME=qr-code-genius-bucket

# --- Application Base URL ---
# The base URL of your deployed application. Used for generating absolute links (e.g., password reset links).
# Example for local development: http://localhost:5000
# Example for production: https://app.qrgenius.com
APP_BASE_URL=http://localhost:5000

# --- Logging Configuration (Optional) ---
# LOG_LEVEL=INFO
# LOG_FILE_PATH=./logs/app.log