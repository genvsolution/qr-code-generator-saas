# General Python files
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
*.so

# Virtual environment
venv/
.venv/
env/
# Specific virtual environment names if used
.direnv/

# Editor/IDE specific files
.idea/             # JetBrains IDEs (PyCharm, IntelliJ)
.vscode/           # VS Code settings
*.swp              # Vim swap files
*~                 # Backup files
.DS_Store          # macOS specific
Thumbs.db          # Windows specific
ehcache/           # Eclipse related

# Environment variables and secrets
.env               # Local environment variables (crucial for secrets)
.flaskenv          # Flask-specific environment variables
instance/          # Flask instance folder, often used for local config or SQLite DBs

# Logs and temporary files
*.log
tmp/
temp/

# Database files (if local SQLite is used for development, though PostgreSQL is primary)
*.sqlite
*.sqlite3

# User-generated content / Uploads
# This directory will store the generated QR code images.
# It should not be tracked by Git as it contains dynamic, user-specific data.
uploads/
media/

# Test and coverage files
.coverage
htmlcov/
.pytest_cache/

# Build artifacts and distribution
build/
dist/
MANIFEST

# Frontend dependencies and build artifacts (for Tailwind CSS, etc.)
# If using Node.js for frontend asset management (e.g., Tailwind CSS compilation)
node_modules/
# Any compiled or generated static assets that are not meant to be version controlled
# For example, if a build process outputs to `static/dist/`
# static/dist/

# Docker specific (if local overrides contain sensitive info or build artifacts)
# docker-compose.override.yml # Uncomment if this file contains local secrets or dev-specific overrides that shouldn't be shared
# .dockerignore # The .dockerignore file itself should be tracked, but build artifacts might be ignored.

# Other common files to ignore
.history/          # VS Code Local History
.terraform/        # Terraform cache
.terraform.lock.hcl # Terraform lock file (often tracked, but can be ignored if dynamic)
*.zip
*.tar.gz
*.rar
*.7z
*.bak
*.tmp
*.dump             # Database dumps

# Jupyter Notebook specific
.ipynb_checkpoints/